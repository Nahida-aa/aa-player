/target
**/*.rs.bk

# 本地环境变量（如 VIDEO=path/to/video.mp4）不进仓库——
# 避免提交本地视频路径。justfile 用 `set dotenv-load` 读取。
.env

# 注意：Cargo.lock **要**提交。
# 本仓库产出的是可执行程序（且计划发布 AUR），锁定依赖版本才能保证
# 构建可复现。"不提交 Cargo.lock" 是纯库(library)的惯例，不适用于此。

.agents/*.local.md
tmp

# 图标生成脚本（bun）的依赖目录；产物在 resources/icons 下随仓库提交。
scripts/node_modules

# makepkg 构建产物（AUR 打包的本地验证），只提交 PKGBUILD 本身。
packaging/aur/*/src/
packaging/aur/*/pkg/
packaging/aur/*/*.pkg.tar.*
packaging/aur/*/*.tar.gz
packaging/aur/*/v0.*
